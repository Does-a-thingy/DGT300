from collation_oliver import *
#from functools import partial

w = Tk()

colour = 'grey'
color = 'blue'

top_f = Frame(w, bg=colour).grid(row=0, column=0, sticky='NESW')
bot_f = Frame(w, bg=color).grid(row=1, column=0, sticky='NESW')
